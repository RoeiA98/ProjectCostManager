services:
  - type: web
    name: my-node-app
    env: node
    buildCommand: echo "No build step"
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
    autoDeploy: true
    publish:
      - path: .